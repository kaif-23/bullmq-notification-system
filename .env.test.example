NODE_ENV=test

# Use a database provisioned specifically for tests. It must already contain the application's base schema.
TEST_DB_HOST=localhost
TEST_DB_PORT=5432
TEST_DB_USER=postgres
TEST_DB_PASSWORD=
TEST_DB_NAME=notification_test

# Use a dedicated Redis instance or a non-default logical database.
TEST_REDIS_HOST=localhost
TEST_REDIS_PORT=6379
TEST_REDIS_DB=15
